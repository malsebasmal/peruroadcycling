<script>
  import Swiper from "swiper";
  import "swiper/css";
  import { Navigation } from "swiper/modules";
  import "swiper/css/navigation";

  // Configurar Swiper
  const swiper = new Swiper(".swiper", {
      loop: true,
      grabCursor: true,
      centeredSlides: true,
      slidesPerView: "auto",
      spaceBetween: 30,
      effect: "coverflow",
      coverflowEffect: {
        rotate: 50, // Grados de rotación de los slides
        stretch: 0, // Espaciado adicional entre slides
        depth: 200, // Profundidad 3D
        modifier: 1, // Multiplicador del efecto
        slideShadows: true, // Sombras para mayor profundidad
      },
      navigation: {
        nextEl: ".swiper-button-next",
        prevEl: ".swiper-button-prev",
      },
    });
</script>

<div class="swiper">
  <div class="swiper-wrapper">
    <div class="swiper-slide">
      <img src="/bike.svg" alt="Slide 1">
    </div>
    <div class="swiper-slide">
      <img src="/bike.svg" alt="Slide 2">
    </div>
    <div class="swiper-slide">
      <img src="/bike.svg" alt="Slide 3">
    </div>
    <div class="swiper-slide">
      <img src="/bike.svg" alt="Slide 4">
    </div>
    <div class="swiper-slide">
      <img src="/bike.svg" alt="Slide 5">
    </div>
  </div>

  <!-- Botones de navegación -->
  <div class="swiper-button-prev"></div>
  <div class="swiper-button-next"></div>
</div>

<style>
  /* Configuración del contenedor Swiper */
  .swiper {
      width: 100%;
      max-width: 1000px;
      height: 500px;
    }

    .swiper-slide {
      display: flex;
      justify-content: center;
      align-items: center;
      background: white;
      border-radius: 20px;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
      overflow: hidden;
      transform-origin: center;
    }

    .swiper-slide img {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }
  
</style>
